<div class="row justify-content-center mt-5">
  <div class="col-md-8 col-lg-6">
    <div class="dashboard-panel-card p-5 text-center shadow-lg position-relative overflow-hidden" style="border-radius: 24px;">
      <!-- Glowing Background Accents -->
      <div class="position-absolute top-0 start-50 translate-middle-x" style="width: 250px; height: 250px; background: radial-gradient(circle, rgba(99, 102, 241, 0.15) 0%, rgba(99, 102, 241, 0) 70%); filter: blur(30px); z-index: 0; pointer-events: none;"></div>

      <div class="position-relative" style="z-index: 1;">
        <!-- Animated Cog Icon -->
        <div class="mx-auto mb-4 d-flex align-items-center justify-content-center text-primary" 
             style="width: 90px; height: 90px; border-radius: 50%; background: rgba(99, 102, 241, 0.08); border: 2px dashed rgba(99, 102, 241, 0.4);">
          <i class="bi bi-gear-fill fs-1 animate-spin" style="display: inline-block;"></i>
        </div>

        <h3 class="fw-bold mb-2 text-gradient" style="font-size: 1.8rem; letter-spacing: -0.5px;">
          Working on settings page
        </h3>
        <p class="text-muted mb-4 px-3" style="font-size: 0.95rem; line-height: 1.6;">
          We are currently designing a state-of-the-art configuration panel for your project workspaces, user permissions, and custom notification systems. Check back soon!
        </p>

        <!-- Progress bar visualization -->
        <div class="px-4 mb-4">
          <div class="progress modern-progress shadow-sm mb-1" style="height: 8px; background: var(--border-color);">
            <div class="progress-bar progress-bar-striped progress-bar-animated" role="progressbar" style="width: 75%; background: linear-gradient(90deg, #6366f1, #8b5cf6);"></div>
          </div>
          <small class="text-muted fw-semibold" style="font-size: 0.75rem;">75% Completed</small>
        </div>

        <div class="mt-4">
          <a href="/" class="btn btn-primary rounded-pill px-4 py-2 fw-semibold shadow-sm gap-2 d-inline-flex align-items-center">
            <i class="bi bi-arrow-left"></i> Back to Dashboard
          </a>
        </div>
      </div>
    </div>
  </div>
</div>

<style>
.animate-spin {
  animation: spin 6s linear infinite;
}
@keyframes spin {
  from { transform: rotate(0deg); }
  to { transform: rotate(360deg); }
}
</style>
